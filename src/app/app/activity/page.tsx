import { createClient } from "@/lib/supabase/server";
import type { JobPost } from "@/lib/types";
import { JobCard } from "@/components/job-card";
import { formatMoney, statusLabel } from "@/lib/format";
import Link from "next/link";
type ActivityProposal = { id:string; post_id:string; amount:number|null; amount_unit:string; message:string; status:string; job_posts?: { id:string; title:string; locality:string; status:string } | null };
export default async function ActivityPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();const [{data:posts},{data:proposals}]=await Promise.all([supabase.from("job_posts").select("*,categories(name,group_name,slug)").eq("author_id",user!.id).order("created_at",{ascending:false}),supabase.from("proposals").select("*,job_posts(id,title,locality,status,category_id,categories(name,group_name,slug))").eq("proposer_id",user!.id).order("created_at",{ascending:false})]);const typedPosts=(posts||[]) as unknown as JobPost[];return <section><div className="appPageTitle"><p className="eyebrow">TU MOVIMIENTO</p><h1>Mi actividad</h1><p>Publicaciones, propuestas y trabajos elegidos en un solo lugar.</p></div><div className="activityStats"><article><strong>{typedPosts.length}</strong><span>Publicaciones</span></article><article><strong>{proposals?.length||0}</strong><span>Propuestas</span></article><article><strong>{typedPosts.filter((p:JobPost)=>p.status==="completed").length}</strong><span>Completadas</span></article></div><div className="sectionTitle"><div><h2>Mis publicaciones</h2><p>Controlá su estado y propuestas.</p></div></div><div className="jobList">{typedPosts.length?typedPosts.map((p:JobPost)=><JobCard key={p.id} post={p} showStatus/>):<div className="emptyState"><h3>Aún no publicaste</h3><Link className="primaryButton small" href="/app/publish">Publicar ahora</Link></div>}</div><div className="sectionTitle spaced"><div><h2>Mis propuestas</h2><p>Seguimiento de tus postulaciones.</p></div></div><div className="proposalList">{proposals?.length?proposals.map((p:ActivityProposal)=><Link href={`/app/jobs/${p.post_id}`} className="proposalCard" key={p.id}><div><span className={`statusBadge ${p.status}`}>{statusLabel(p.status)}</span><h3>{p.job_posts?.title||"Publicación"}</h3></div><p>{p.message}</p><strong>{formatMoney(p.amount,p.amount,p.amount_unit==="open"?"open":"fixed")}</strong></Link>):<div className="emptyState"><h3>Aún no enviaste propuestas</h3><Link className="primaryButton small" href="/app/jobs">Buscar oportunidades</Link></div>}</div></section>}
